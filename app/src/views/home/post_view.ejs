<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시글 보기</title>
    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif;
            background-color: #333;
            margin: 0;
            padding: 0;
            color: #fff;
        }

        .header {
            width: 100%;
            background-color: #333;
            padding: 10px 0;
            text-align: left;
        }

        .header a.logo {
            font-size: 30px;
            color: #ffcc00;
            font-weight: bold;
            text-decoration: none;
            padding-left: 20px;
        }

        .separator {
            border-top: 2px solid #ffcc00;
            margin-bottom: 20px;
        }

        .container {
            width: 80%;
            max-width: 900px;
            margin: 0 auto;
            background-color: #444;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            position: relative;
        }

        .post-header {
            margin-bottom: 20px;
        }

        .post-title {
            font-size: 28px;
            font-weight: bold;
            color: #ffcc00;
        }

        .post-meta {
            font-size: 14px;
            color: #999;
        }

        .post-content {
            font-size: 18px;
            color: #ddd;
            margin-top: 20px;
            line-height: 1.6;
        }

        .form-actions {
            margin-top: 20px;
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }

        button {
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-left: 10px;
        }

        .button-scrap {
            background-color: #ffcc00;
            color: #333;
            font-weight: bold;
        }

        .button-list {
            background-color: #999;
            color: #fff;
        }

        .button-delete {
            background-color: #ff4444;
            color: #fff;
            font-weight: bold;
        }

        button:hover {
            opacity: 0.9;
        }

        .comment-section {
            margin-top: 30px;
        }

        .comment-section textarea {
            width: 97%;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #555;
            background-color: #555;
            color: #fff;
            font-size: 16px;
            margin-bottom: 10px;
            resize: none;
        }

        .comment-section button {
            padding: 10px 20px;
            background-color: #ffcc00;
            color: #333;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        input[type="text"]:focus,
        textarea:focus {
            border-color: #ffcc00;
            outline: none;
        }

        .comment-section button:hover {
            opacity: 0.9;
        }
    </style>
</head>
<body>

    <!-- 헤더: KUCIS 로고 -->
    <div class="header">
        <a href="/login" class="logo">KUCIS</a>
    </div>

    <!-- 구분선 -->
    <div class="separator"></div>

    <!-- 게시물 내용 컨테이너 -->
    <div class="container">
        <div class="post-header">
            <div class="post-title" id="postTitle"><%= post.title %></div>
            <div class="post-meta"><%= post.id %> | <%= new Date(post.created_at).toLocaleString() %></div>
        </div>

        <div class="post-content" id="postContent">
            <%= post.content %>
        </div>

        <div class="form-actions">
            <!-- 스크랩, 글 목록, 삭제 버튼 -->
            <button class="button-scrap" onclick="scrapPost()">스크랩</button>
            <button class="button-list" onclick="location.href='/community'">글 목록</button>
            <button class="button-delete" onclick="deletePost('<%= post.postnum %>')">삭제</button>
        </div>

        
        <div class="comment-section">
            <textarea id="comment" rows="4" placeholder="댓글을 입력하세요."></textarea>
            <button>댓글 작성</button>
        </div>
    </div>

    <script>
        // 게시글 삭제 기능 구현
        function deletePost(postId) {
            if (confirm("정말로 이 게시글을 삭제하시겠습니까?")) {
                fetch(`/delete-post/${postId}`, {
                    method: 'DELETE',
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        alert("게시글이 삭제되었습니다.");
                        window.location.href = '/community';  // 삭제 후 게시글 목록으로 이동
                    } else {
                        alert("게시글 삭제에 실패했습니다.");
                    }
                })
                .catch(err => {
                    console.error(err);
                    alert("서버 오류가 발생했습니다.");
                });
            }
        }
    </script>

</body>
</html>
