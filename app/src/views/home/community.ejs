<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시판 목록</title>
    <link rel="stylesheet" href="/css/home/community.css">
</head>
<body>

    <!-- 헤더: KUCIS 로고 -->
    <div class="header">
        <a href="/login" class="logo">NGDB</a>
    </div>

    <!-- 구분선 -->
    <div class="separator"></div>

    <!-- 게시판 목록 컨테이너 -->
    <div class="container">
        <h2>게시판 목록</h2>
        <div id="postList">
            <% if (posts.length > 0) { %>
                <% posts.forEach(function(post) { %>
                    <div class="post-item">
                        <div class="post-title"><a href="/post/<%= post.postnum %>"><%= post.title %></a></a></div>
                        <div class="post-content"><%= post.content %></div>
                        <div class="post-meta">작성자: <%= post.id %> | <%= new Date(post.created_at).toLocaleString() %></div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="no-posts">게시물이 없습니다</div>
            <% } %>
        </div>        
        <div class="form-actions">
            <button onclick="location.href='/board'">글 작성하기</button>
        </div>
    </div>

</body>
</html>
