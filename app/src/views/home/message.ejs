<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>쪽지함</title>
    <link rel="stylesheet" href="/css/home/message.css" />
    <!-- 외부 CSS 파일을 링크 -->
  </head>
  <body>
    <div class="header">
      <a href="/login" class="logo">KUCIS</a>
    </div>

    <div class="separator"></div>

    <div class="container">
      <div class="form-title">내 쪽지 리스트</div>

      <ul class="message-list">
        <% if (messages.length > 0) { %> <% messages.forEach(function(message) {
        %>
        <li class="message-item">
          <span class="message-title"><%= message.postnum %></span>
          <!-- 쪽지 제목 -->
          <span class="message-time"><%= message.created_at %></span>
          <!-- 쪽지 시간 -->
          <div class="message-actions">
            <button class="button-view">보기</button>
            <button class="button-delete">삭제</button>
          </div>
        </li>
        <% }); %> <% } else { %>
        <li class="message-item">받은 쪽지가 없습니다.</li>
        <% } %>
      </ul>
    </div>
  </body>
</html>
